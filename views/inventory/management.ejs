<%# Content Heading %>

<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<% messages() %>


<div>
  <a href="/inv/add-inventory" class="button">Add New Vehicle</a>
  <a href="/inv/add-classification" class="button">Add New Classification</a>
</div>